apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "application.fullname" . }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-4"
data:
{{- with .Values.app.env }}
  {{- toYaml . | nindent 2 }}
{{- end }}
