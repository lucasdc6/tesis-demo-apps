apiVersion: v1
kind: Secret
metadata:
  name: {{ include "application.fullname" . }}-nginx
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-4"
type: Opaque
stringData:
{{- with .Values.nginx.secretEnv }}
  {{- toYaml . | nindent 2 }}
{{- end }}
